interface Brand {
  title: string
  description?: string
  pictures?: {
    id: string,
    name: string,
    ext: string,
    mime: steing,
    size: number,
    url: string,
    createdAt: string,
    updatedAt: string
  }[]
}